services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    networks:
      - jetnet

  frontend:
    build:
      context: ./frontend
    ports:
      - "8081:80"
    networks:
      - jetnet
    depends_on:
      - backend

networks:
  jetnet:
    driver: bridge
